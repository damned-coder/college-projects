#include <stdio.h>
#include <conio.h>
#include <string.h>
void main()
{
    char s[100], a[100];
    int i, original, reverse;
    printf("Enter any string:");
    gets(s);


        strrev(s);

    printf("%s", strrev(s));
}
